import { defineMessages } from 'react-intl';

export const scope = 'Layouts.NavBar';

export default defineMessages({
  buttonCategories: {
    id: `${scope}.button.categories`,
    defaultMessage: 'Categories',
  },
});
