import { defineMessages } from 'react-intl';

export const scope = 'Containers.Header';

export default defineMessages({
  deleteMessage: {
    id: `${scope}.deleteMessage`,
    defaultMessage: 'Delete successfully',
  },
});
