import { defineMessages } from 'react-intl';

export const scope = 'Containers.ProductSearch';

export default defineMessages({
  showing: {
    id: `${scope}.showing`,
    defaultMessage: 'Showing',
  },
  of: {
    id: `${scope}.of`,
    defaultMessage: 'of',
  },
});
