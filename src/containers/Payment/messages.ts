import { defineMessages } from 'react-intl';

export const scope = 'Containers.Payment';

export default defineMessages({
  paymentFailedMessage: {
    id: `${scope}.paymentFailedMessage`,
    defaultMessage: 'Payment failed',
  },
});
