import { defineMessages } from 'react-intl';

export const scope = 'Containers.WishList';

export default defineMessages({
  wishList: {
    id: `${scope}.wishList`,
    defaultMessage: 'My Wish List',
  },
  btnAddAllToCart: {
    id: `${scope}.button.addAllToCart`,
    defaultMessage: 'Add All To Cart',
  },
});
