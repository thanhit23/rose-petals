import { defineMessages } from 'react-intl';

export const scope = 'Containers.SupportTicket';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'Support Ticket',
  },
  btnBackToSupport: {
    id: `${scope}.button.backToSupportTicket`,
    defaultMessage: 'Back to Support Ticket',
  },
});
