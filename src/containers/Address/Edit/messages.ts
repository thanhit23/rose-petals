import { defineMessages } from 'react-intl';

export const scope = 'Components.AddressEdit';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'Edit Address',
  },
  btnBackAddress: {
    id: `${scope}.button.backAddress`,
    defaultMessage: 'Back To Address',
  },
});
