import { defineMessages } from 'react-intl';

export const scope = 'Containers.AddressAdd';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'Add Address',
  },
  btnBackAddress: {
    id: `${scope}.button.backAddress`,
    defaultMessage: 'Back To Address',
  },
});
