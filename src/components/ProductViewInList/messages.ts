import { defineMessages } from 'react-intl';

export const scope = 'Components.ProductViewInList';

export default defineMessages({
  btnAddToCart: {
    id: `${scope}.button.addToCart`,
    defaultMessage: 'Add To Cart',
  },
});
