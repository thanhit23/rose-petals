import { defineMessages } from 'react-intl';

export const scope = 'Components.Home.NavBar';

export default defineMessages({
  buttonCategories: {
    id: `${scope}.button.categories`,
    defaultMessage: 'Categories',
  },
});
