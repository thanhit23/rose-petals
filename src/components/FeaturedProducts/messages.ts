import { defineMessages } from 'react-intl';

export const scope = 'Components.FeatureProducts';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'Featured Products',
  },
});
