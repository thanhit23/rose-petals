const container = {
  marginTop: '64px',
};

const dividerTop = {
  marginBottom: '32px',
  borderColor: '#DAE1E7',
};

const dividerBottom = {
  marginTop: '32px',
  borderColor: '#DAE1E7',
};

const boxSlide = {
  minWidth: 0,
  position: 'relative',
};

const boxImg = {
  display: 'flex',
  maxWidth: '110px',
  height: '100%',
  margin: 'auto',
};

const img = {
  width: '100%',
  objectFit: 'contain',
  filter: 'grayscale(1)',
};

export default {
  dividerBottom,
  dividerTop,
  container,
  boxSlide,
  boxImg,
  img,
};
