import { defineMessages } from 'react-intl';

export const scope = 'Components.ProductDescription';

export default defineMessages({
  specification: {
    id: `${scope}.specification`,
    defaultMessage: 'Specification',
  },
});
