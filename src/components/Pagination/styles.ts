import { styles } from '../common';

const boxPagination = {
  display: 'flex',
  marginTop: '40px',
  justifyContent: 'center',
  '& > nav > ul > li > button': {
    fontFamily: styles.fontFamilyDefault,
  },
};

export default {
  boxPagination,
};
