import { defineMessages } from 'react-intl';

export const scope = 'Components.BreadCrumb';

export default defineMessages({
  home: {
    id: `${scope}.home`,
    defaultMessage: 'Home',
  },
});
