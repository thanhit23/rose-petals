import { defineMessages } from 'react-intl';

export const scope = 'Components.LatestArticles';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'Latest Articles',
  },
  readMore: {
    id: `${scope}.readMore`,
    defaultMessage: 'Read More',
  },
});
