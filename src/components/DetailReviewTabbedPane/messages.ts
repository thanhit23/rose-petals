import { defineMessages } from 'react-intl';

export const scope = 'Components.DetailReviewTabbedPane';

export default defineMessages({
  description: {
    id: `${scope}.description`,
    defaultMessage: 'Description',
  },
  reviews: {
    id: `${scope}.reviews`,
    defaultMessage: 'Reviews',
  },
});
