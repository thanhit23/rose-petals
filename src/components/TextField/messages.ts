import { defineMessages } from 'react-intl';

export const scope = 'Components.TextField';

export default defineMessages({
  default: {
    id: `${scope}.default`,
    defaultMessage: '',
  },
});
