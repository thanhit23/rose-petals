import { styles } from '../common';

const textField = {
  '& > label': {
    fontSize: '14px',
    fontFamily: styles.fontFamilyDefault,
    letterSpacing: 'inherit',
  },
  '& > div': {
    fontSize: '14px',
    fontFamily: styles.fontFamilyDefault,
    letterSpacing: 'inherit',
  },
};

export default {
  textField,
};
