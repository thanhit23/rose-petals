import { defineMessages } from 'react-intl';

export const scope = 'Components.ProductItem';

export default defineMessages({
  btnQuickView: {
    id: `${scope}.button.QuickView`,
    defaultMessage: 'Quick View',
  },
  review: {
    id: `${scope}.review`,
    defaultMessage: 'Review',
  },
});
