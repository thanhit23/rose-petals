import { defineMessages } from 'react-intl';

export const scope = 'Components.NotFound';

export default defineMessages({
  btnGoBack: {
    id: `${scope}.button.goBack`,
    defaultMessage: 'Go Back',
  },
  btnGoToHome: {
    id: `${scope}.button.goToHome`,
    defaultMessage: 'Go To Home',
  },
});
