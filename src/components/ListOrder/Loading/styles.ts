const wrapperSkeleton = {
  boxShadow: '0px 1px 3px #bbbbbb',
  borderRadius: '8px',
};

const skeletonItem = {
  marginTop: '16px',
  borderRadius: '8px',
  opacity: '.8',
};

export default {
  skeletonItem,
  wrapperSkeleton,
};
