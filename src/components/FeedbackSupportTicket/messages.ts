import { defineMessages } from 'react-intl';

export const scope = 'Components.FeedbackSupportTicket';

export default defineMessages({
  postMessage: {
    id: `${scope}.button.postMessage`,
    defaultMessage: 'Post Message',
  },
});
