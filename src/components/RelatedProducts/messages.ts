import { defineMessages } from 'react-intl';

export const scope = 'Components.ProductDetail.RelatedProducts';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'Related Products',
  },
});
