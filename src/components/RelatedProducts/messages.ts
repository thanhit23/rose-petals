import { defineMessages } from 'react-intl';

export const scope = 'Components.ProductDetailPage.RelatedProducts';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'Related Products',
  },
});
