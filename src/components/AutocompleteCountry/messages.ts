import { defineMessages } from 'react-intl';

export const scope = 'Components.AutocompleteCountry';

export default defineMessages({
  labelCountry: {
    id: `${scope}.label.country`,
    defaultMessage: 'Country',
  },
});
