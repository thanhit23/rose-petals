import { defineMessages } from 'react-intl';

export const scope = 'Components.ProductDetailPage';

export default defineMessages({
  specification: {
    id: `${scope}.specification`,
    defaultMessage: 'Specification',
  },
});
